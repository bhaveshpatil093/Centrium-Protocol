import {web3} from 'hardhat'
import artifacts from '../artifacts/contracts/Lock.sol/Lock.json'

async function main() {
    
}

main().catch(error => {
    console.error(error);
    process.exitCode = 1;
});
